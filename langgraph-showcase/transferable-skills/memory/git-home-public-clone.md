---
name: git-home-public-clone
description: Git home is public-git/agentic-security-test-sdk; this LangGraph app lives in langgraph-showcase/ (2026-09-25)
metadata:
  type: project
---

v0.0.1 lives in `/Users/joaocerqueira/Documents/git/micro-cosmos/public-git/agentic-security-test-sdk`. This LangGraph deployment is the `langgraph-showcase/` subfolder. The ADK deployment is `google-cloud-showcase/`. Commit in that public repo; do not `git init` a working folder unless asked.

Clone-ready means, inside this folder: Dockerfile, compose, `.env.example`, `examples/`, `images/`, `tests/`, package data for templates/static, LICENSE. Do not commit `.venv`, `.env`, or new `runs/*` (keep `runs/.gitkeep`). Screenshots in `images/` are part of the product README and must be in git.
