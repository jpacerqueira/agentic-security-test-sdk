---
name: git-home-public-clone
description: Git clone/commit home is public-git/agentic-security-test-sdk; the sibling working tree may stay file-only (2026-09-17)
metadata:
  type: project
---

v0.0.1 lives in `/Users/joaocerqueira/Documents/git/micro-cosmos/public-git/agentic-security-test-sdk`. Port application files there and commit; do not `git init` the working folder unless asked.

Clone-ready means: Dockerfile, compose, `.env.example`, `examples/`, `images/`, `tests/`, package data for templates/static, LICENSE. Do not commit `.venv`, `.env`, or `runs/*` (keep `runs/.gitkeep`). Screenshots in `images/` are part of the product README and must be in git.
